name: Auto Scan IPTV Channels

on:
  schedule:
    # 每天北京时间早上 8:00 自动运行一次 (UTC 0:00)
    - cron: '0 0 * * *'
  workflow_dispatch:
    # 允许你在 GitHub 页面上手动点击按钮触发运行

jobs:
  scan_and_update:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        # 安装 requests 库，用于在脚本中联网获取最新的动态台标映射表
        pip install requests

    - name: Run Scanner Script
      run: python scanner.py

    - name: Commit and Push M3U File
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        
        # 检查 live.m3u 文件是否有变动，避免无意义的重复提交
        if [ -n "$(git status --porcelain live.m3u)" ]; then
          git add live.m3u
          git commit -m "自动更新：获取最新动态台标并生成四季源"
          git push
        else
          echo "直播源链接无变动，跳过本次提交。"
        fi
